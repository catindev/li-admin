import styles from './loader-layout.less';

export default angular

  .module('Loader Layout Module', [])

  .component('loaderLayout', { template: `<div class="${styles.loader}"></div>` })
  
  .name;
